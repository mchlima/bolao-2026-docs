# Pendências técnicas a decidir

Registradas ao longo do design. Resolver e mover para os contratos/ADRs correspondentes.

| # | Pendência | Status | Decisão |
|---|---|---|---|
| 1 | **Valores numéricos** de cada tier de pontuação. | ✅ Resolvida (revista) | **Modelo B (proximidade):** acertou o vencedor → base **4**; por time: cravou +3, errou por 1 +1; errou o vencedor → 0 (cravar = 10). Tiers viraram rótulos: `EXACT`/`ONE_TEAM_SCORE`/`CLOSE`/`OUTCOME`/`NONE`. Env `SCORING_BASE`/`TEAM_EXACT`/`TEAM_NEAR`. **Desempate:** palpite mais antigo (`createdAt`) acima. Substituiu o esquema em camadas. Ver `api/scoring.md` |
| 2 | **Estilo de paginação por tela**: offset+total (admin) vs cursor (feeds). | ⏳ Aberta | Esboço em `api/contracts.md` |
| 3 | **Intervalo de polling** das telas LIVE (placar + ranking provisório). | ✅ Resolvida (revista) | **Removido** (a pedido): o auto-refresh saiu de todas as telas; atualização ao vivo agora é manual (recarregar). `useLivePolling`/`LIVE_POLL_MS` deletados. |
| 4 | **"Gerar nova senha"**: exibir senha temporária **ou** disparar fluxo de redefinição. | 🟡 Parcial | MVP: **gera senha temporária** retornada 1x ao admin (sem serviço de e-mail ainda). Senha **não** vai para o audit log. Migrar p/ fluxo de reset-link quando houver e-mail. |
| 5 | **Pódio no ranking**: condicionar a um nº mínimo de participantes. | ✅ Resolvida | Pódio só aparece com **≥ 3 participantes** (ranking do torneio e da partida); abaixo disso, lista simples. |
| 6 | **Política de uso de escudos de clubes** (marca/IP). Fora do escopo técnico; registrar. | ⏳ Aberta | — |
| 7 | **Conjunto final de `TournamentStatus`** (além de `DRAFT`). | ⏳ Aberta | — |
| 8 | **Storage de objetos**: Cloudflare R2 vs S3. | ✅ Resolvida | **Cloudflare R2** (S3-compatible). Backend gera URLs e otimiza imagens (`sharp`→WebP). Vars `STORAGE_*` no `.env`. |
| 9 | **Nome final do app** ("Amigos do Bolão" a confirmar). | ⏳ Aberta | — |
| 10 | **Versão do Nuxt**: briefing pede Nuxt 3; tooling atual já default p/ Nuxt 4. Scaffold feito em **Nuxt 3** — confirmar se mantém. | ⏳ Aberta | Scaffold = Nuxt 3 |
| 11 | **Hash de senha**: bcrypt vs argon2. | ✅ Resolvida | **bcryptjs** (JS puro, 10 rounds) — sem compilação nativa, mantém a imagem Docker `node:slim` enxuta |
| 12 | **Regra de bloqueio de palpite** por `MatchStatus` (até kickoff? até LIVE?). | ✅ Resolvida (revista 2x) | Automático: aceita se `SCHEDULED` E `now < kickoffAt` E times definidos. **Trava DURA no kickoff (2026-06-12):** o override `Match.predictionsOpen` só fecha **cedo** (`false`); `null`/`true` mantêm aberto **só até o kickoff** — nunca segura aberto durante/depois do início (justiça: ninguém palpita vendo o placar). Estados terminais/não-`SCHEDULED` nunca aceitam. Eventos de fechamento no kickoff via `MatchWindowService`. Códigos `PREDICTION_LOCKED` / `MATCH_NOT_OPEN`. _(Antes: override podia manter aberto em LIVE — removido.)_ |
| 13 | **Times da partida nullable** (briefing assumia obrigatório). Necessário p/ mata-mata "a definir". | ✅ Resolvida | `homeTeamId`/`awayTeamId` nullable + `homeSourceLabel`/`awaySourceLabel` (slot TBD). Palpite só liberado quando os dois times estão definidos. |
| 14 | **Horários de kickoff da Copa** (grupos derivados/fuso; mata-mata placeholder). | 🟡 Parcial | Aplicado **+1h** em todos (estavam 1h adiantados, confirmado pelo usuário). UTC no banco; exibição no **fuso da conta** (`User.timezone`, default `America/Sao_Paulo`). Ainda best-effort — admin pode ajustar por partida. |
| 15 | **Deploy e região do banco.** | ✅ Resolvida | API em **VPS Locaweb (São Paulo)** via Docker Compose + Nginx, atrás do Cloudflare (**Flexible**); web na **Vercel**; domínios `bolao2026`/`api-bolao2026` em `kratinho.com.br`. Banco **migrado `us-east-1` → `sa-east-1`** por latência (131ms → ~2ms RTT). Ver `deploy/vps-ubuntu.md`. |
| 16 | **Placar parcial ao vivo com lado `null`.** | ✅ Resolvida | Em `LIVE`/`FINISHED`, um placar `null` conta como **0** (ex.: 1-0 com o visitante intocado) — rankings provisórios e exibição (card/detalhe) usam o placar coalescido. |
| 17 | **Banco de dev/staging separado do de produção.** | ✅ Resolvida (2026-06-12) | Criado **2º projeto Supabase em São Paulo** (`sa-east-1`, ref `ooukpcqycmmgixvtrkcm`, PG 17.6) como dev/staging e **espelhado de prod** via `pg_dump --schema=public` → `psql` restore (idêntico: 11 tabelas, contagens iguais, 11 migrations, `migrate status` up-to-date). O `.env` **de dev** aponta pra ele (`DATABASE_URL`/`DIRECT_URL`); **`.env` de prod (no VPS) inalterado**. Robô ESPN segue só em `NODE_ENV=production`. Também criadas branches `development` nos 3 repos. _(Projeto antigo `us-east-1` não reusado — latência.)_ |
| 18 | **Consolação por cravar os gols de um time errando o vencedor.** | ✅ Resolvida (2026-06-12) | Antes, errar o vencedor zerava a partida mesmo cravando o número de gols de um time (ex.: palpite 2×1 num 0×1 — acertou o 1 do visitante, mas 0 pontos). Agora rende **TEAM_EXACT_MISS = 1** ponto (env `SCORING_TEAM_EXACT_MISS`), **só** se o número for **exato** (errar por 1 fora do portão = 0), e **estritamente abaixo da BASE** (acertar o vencedor sempre vale mais). Novo `tier` `TEAM_GOALS` ("Gols de um time", cor scarlet). Pontos são derivados → vale retroativo sem backfill. |
| 19 | **Estrutura de competições** (grupos, chaves, ida/volta, classificação, rodadas, 3º lugar). | ✅ Resolvida (2026-06-13) | Modelo dos provedores (Opta/Sportradar/Sportmonks): hierarquia **`Competition → Season → Stage → (Group \| Round) → Tie → Match`**. **Rename completo `Tournament → Season`** (preservando dados via migração hand-edited — RENAME de tabela/colunas/enum, sem DROP) + nova **`Competition`** atemporal que guarda o **`espnLeagueSlug`** (absorve a ideia R1 do roadmap = robô multi-torneio). **`Tie`** = nó do bracket acima de 1–2 jogos (ida/volta), com agregado/pênaltis/vencedor/resolução. Slots TBD do chaveamento = **feeders tipados** (`GROUP_POSITION`/`BEST_RANKED`/`MATCH_WINNER`/`MATCH_LOSER`) com resolução **híbrida** (auto via `SlotResolverService` + override admin). **Melhor-3º da Copa 2026 AUTO-RESOLVIDO** pela **tabela Anexo C da FIFA** (495 combinações = C(12,8), determinística, sem sorteio secundário; parseada por código do template oficial da Wikipédia — `wc2026-thirds-table.ts` — não transcrita à mão). Ranking dos 3º por pontos→saldo→gols→**fair play** (cartões AUTOMÁTICOS desde 2026-06-13: o robô ESPN grava amarelos/vermelhos+pontos de fair play por partida; só o critério final ranking-FIFA/sorteio fica no override admin — ver `architecture/espn-fairplay-spike.md`). O `winnerGroup` marcado em cada feeder dá a coluna da tabela. **Classificação calculada** (não armazenada) pelo `StandingsService`: P/J/V/E/D/GP/GC/SG/% + últimos-5, desempate por **preset por Stage** (FIFA/BRASILEIRAO/UEFA/CONMEBOL/GENERIC, com confronto-direto). Escopo: só estrutura + visualização — `ScoringService`/ranking de palpiteiros **intocados**. UI: páginas de Classificação e Chaveamento + admin CRUD de estrutura. Ver `database/schema.md`. |
